


// Instantiate the module
scrolling_display instance_name (
    .ascii(ascii), 
    .dsn(dsn), 
    .clearn(clearn), 
    .clk(clk), 
    .rstn(rstn), 
    .segments(segments), 
    .digit_select(digit_select), 
    .leds(leds)
    );


